{
    "rules": {
        "properties": {
            "private": true,
            "required": ["description", "main", "author", "license", "publishConfig", "main", "bin", "files", "ava", "repository"]
        },
        "scripts": {
            "allow": ["node", "npm", "ava", "webpack", "ts-node"]
        },
        "dependencies": {
            "checkLatest": true
        }
    }
}
